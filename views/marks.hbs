<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SAMS:MARKS</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous" />

    <link rel="stylesheet" href="/css/style.css">
    <style>
        #head {
            margin-left: 9rem;
        }


        .row {
            display: flex;
        }

        .row .sgpa {
            margin-left: 940px;
        }

        .main {
            max-width: 80vw;
            min-height: 80vh;
            border: 2px solid black;
            margin: 5%;
            padding: 5px;
            background-color: #f1f1f1;
        }

        /* Style the button that is used to open and close the collapsible content */
        .collapsible {
            background-color: #eee;
            color: #444;
            cursor: pointer;
            padding: 18px;
            width: 100%;
            border: none;
            text-align: left;
            outline: none;
            font-size: 15px;
            transition: 0.5s ease;
        }

        /* Add a background color to the button if it is clicked on (add the .active class with JS), and when you move the mouse over it (hover) */
        .active,
        .collapsible:hover {
            background-color: #ccc;
        }

        /* Style the collapsible content. Note: hidden by default */
        .content {
            padding: 0 18px;
            display: none;
            overflow: hidden;
            max-height: 0;
            background-color: #f1f1f1;
            transition: max-height 0.2s ease-out;
        }


        .btn {
            padding: 0.5rem;
            margin: 6px;

        }

        .add-cont {
            z-index: 1;
            border: 2px solid black;
            width: 30rem;
            height: 30rem;
        }

        .add-cont {
            z-index: 2;
            /* Set a higher z-index for add-cont to make it appear above main */
            border: 2px solid black;
            width: 30rem;
            height: 30rem;
            position: absolute;
            /* Position the add-cont div */
            top: 50%;
            /* Position it in the middle of the viewport */
            left: 50%;
            /* Position it in the middle of the viewport */
            transform: translate(-50%, -50%);
            /* Center the add-cont div */
            background-color: #444;
            color: white;
            border-radius: 5px;
            display: flex;
            flex-direction: column;
            visibility: hidden;
        }

        .main {
            max-width: 80vw;
            min-height: 80vh;
            border: 2px solid black;
            margin-left: auto;
            margin-right: auto;
            padding: 5px;
            position: relative;
            /* Set position to relative */
            z-index: 1;
            /* Set a lower z-index for main to place it behind add-cont */
            overflow: scroll;
        }

        .add-cont h2 {
            margin-left: 2rem;
        }

        .add-cont hr {
            margin: 0;
        }

        .input-cont {
            width: 30rem;
            height: 20rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        .input {
            width: 300px;
            height: 30px;
            margin: 5px;
            border-radius: 5px;
        }

        .add-btn {
            font-size: 1.2rem;
            cursor: pointer;
            width: 70px;
            height: 30px;
            margin-left: auto;
            margin-right: auto;
            border-radius: 5px;

        }

        .cancel-btn {
            font-size: 1.1rem;
            cursor: pointer;
            width: 70px;
            height: 30px;
            margin-right: auto;
            border-radius: 5px;

        }

        .add-btns {
            display: flex;
        }

        .marks-table {
            width: 100%;
            margin-top: 20px;
            border-collapse: collapse;
        }

        .marks-table th,
        .marks-table td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }

        .marks-table th {
            background-color: #f2f2f2;
        }
    </style>
</head>

<body>
    <nav>
        <h3><a href="/dashboard">SAMS</a></h3>
        <ul>
            <li><a href="/marks">MARKS</a></li>
            <li><a href="/projects">PROJECTS</a></li>
            <li><a href="/achievement">ACHIEVEMENTS</a></li>

        </ul>
    </nav>
    {{#if message}}
    <h6 class="alert alert-danger mt-4 mb-4">{{message}}</h6>
    {{/if}}
    <div class="container">
        <div class="add-cont">
            <h2>Add Subject</h2>
            <hr>
            <div class="input-cont">
                <input type="text" class="input" placeholder="Enter Subject Name" name="Sname" required>
                <input type="number" class="input" placeholder="Enter Max Name" name="maxMarks" min="0" max="100"
                    pattern="\d*" required>
                <input type="number" class="input" placeholder="Enter Subject Marks" name="minMarks" min="0" max="100"
                    pattern="\d*" required>
                <input type="text" class="input" placeholder="Enter Subject Grade" name="SGrade" required>
            </div>
            <div class="add-btns">
                <button type="submit" class="add-btn">Add</button>
                {{!-- <button class="cancel-btn">Cancel</button> --}}
            </div>
        </div>
        <div class="main">
            <button type="button" class="collapsible">Semester 1</button>
            <!-- Within each semester collapsible section -->
            <div class="content">
                <div class="items">
                    <table class="marks-table">
                        <thead>
                            <tr>
                                <th>Subject Name</th>
                                <th>Max Marks</th>
                                <th>Subject Marks</th>
                                <th>Subject Grade</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
                <button class="btn">Add Subject</button>
            </div>
            <button type="button" class="collapsible">Semester 2</button>
            <!-- Within each semester collapsible section -->
            <div class="content">
                <div class="items">
                    <table class="marks-table">
                        <thead>
                            <tr>
                                <th>Subject Name</th>
                                <th>Max Marks</th>
                                <th>Subject Marks</th>
                                <th>Subject Grade</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
                <button class="btn">Add Subject</button>
            </div>

            <button type="button" class="collapsible">Semester 3</button>
            <!-- Within each semester collapsible section -->
            <div class="content">
                <div class="items">
                    <table class="marks-table">
                        <thead>
                            <tr>
                                <th>Subject Name</th>
                                <th>Max Marks</th>
                                <th>Subject Marks</th>
                                <th>Subject Grade</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
                <button class="btn">Add Subject</button>
            </div>
            <button type="button" class="collapsible">Semester 4</button>
            <!-- Within each semester collapsible section -->
            <div class="content">
                <div class="items">
                    <table class="marks-table">
                        <thead>
                            <tr>
                                <th>Subject Name</th>
                                <th>Max Marks</th>
                                <th>Subject Marks</th>
                                <th>Subject Grade</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
                <button class="btn">Add Subject</button>
            </div>
            <button type="button" class="collapsible">Semester 5</button>
            <!-- Within each semester collapsible section -->
            <div class="content">
                <div class="items">
                    <table class="marks-table">
                        <thead>
                            <tr>
                                <th>Subject Name</th>
                                <th>Max Marks</th>
                                <th>Subject Marks</th>
                                <th>Subject Grade</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
                <button class="btn">Add Subject</button>
            </div>
            <button type="button" class="collapsible">Semester 6</button>
            <!-- Within each semester collapsible section -->
            <div class="content">
                <div class="items">
                    <table class="marks-table">
                        <thead>
                            <tr>
                                <th>Subject Name</th>
                                <th>Max Marks</th>
                                <th>Subject Marks</th>
                                <th>Subject Grade</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
                <button class="btn">Add Subject</button>
            </div>
            <button type="button" class="collapsible">Semester 7</button>
            <!-- Within each semester collapsible section -->
            <div class="content">
                <div class="items">
                    <table class="marks-table">
                        <thead>
                            <tr>
                                <th>Subject Name</th>
                                <th>Max Marks</th>
                                <th>Subject Marks</th>
                                <th>Subject Grade</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
                <button class="btn">Add Subject</button>
            </div>
            <button type="button" class="collapsible">Semester 8</button>
            <!-- Within each semester collapsible section -->
            <div class="content">
                <div class="items">
                    <table class="marks-table">
                        <thead>
                            <tr>
                                <th>Subject Name</th>
                                <th>Max Marks</th>
                                <th>Subject Marks</th>
                                <th>Subject Grade</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
                <button class="btn">Add Subject</button>
            </div>

            <!-- Add other semesters here -->

        </div>
    </div>
    <footer>
        <div>© 2021 - Made with &hearts; by Mausooq & Nafih</div>
        <a href="/auth/logout">Logout</a>
    </footer>

    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const btns = document.querySelectorAll(".btn");
            const cancelbtns = document.querySelectorAll(".cancel-btn");

            btns.forEach(btn => {
                btn.addEventListener("click", () => {
                    const addCont = document.querySelector(".add-cont");
                    addCont.style.visibility = "visible"; // Change visibility to "visible"
                    addCont.dataset.semester = btn.parentElement.previousElementSibling.textContent.trim();
                });
            });

            cancelbtns.forEach(btn => {
                btn.addEventListener("click", () => {
                    const addCont = document.querySelector(".add-cont");
                    addCont.style.visibility = "hidden"; // Change visibility to "hidden"
                });
            });

            // Function to add marks to the table
            function addMarks() {
                // Get input values
                 const semester = document.querySelector(".add-cont").dataset.semester;
                const subjectName = document.querySelector(".input-cont input[type='text']").value;
                const maxMarks = document.querySelector(".input-cont input[type='number']").value;
                const subjectMarks = document.querySelector(".input-cont input[type='number']:nth-of-type(2)").value;
                const subjectGrade = document.querySelector(".input-cont input[type='text']").value;
                if (subjectName === "" || subjectMarks === "" || subjectGrade === "" || maxMarks === "") {
                    return;
                }

                // Create a new row for the table
                const newRow = document.createElement("tr");
                newRow.innerHTML = `
                  <td>${subjectName}</td>
                  <td>${maxMarks}</td>
                  <td>${subjectMarks}</td>
                  <td>${subjectGrade}</td>
              `;

                // Find the current active content (associated with the clicked semester)
                const activeContent = document.querySelector(".collapsible.active").nextElementSibling.querySelector(".items table tbody");
               
               
                // Add the new row to the table
                activeContent.appendChild(newRow);

                // Get the height of the table's content
                const tableHeight = activeContent.getBoundingClientRect().height;

                // Set the height of the content area
                const content = activeContent.parentElement;
                content.style.height = `${tableHeight}px`;

                // Clear input fields
                document.querySelectorAll(".input-cont input").forEach(input => input.value = "");
                fetch('http://localhost:5000/auth/addSubject', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({
                        semester,
                        subjectName,
                        maxMarks,
                        subjectMarks,
                        subjectGrade,
                    }),
                })
                    .then((response) => response.json())
                    .then((data) => {
                        console.log(data);
                    })
                    .catch((error) => {
                        console.error('Error:', error);
                    });
            }

            // Add event listener to the "Add" button
            document.querySelectorAll(".add-btn").forEach(btn => {
                btn.addEventListener("click", () => {
                    addMarks();
                    document.querySelector(".add-cont").style.visibility = "hidden";
                });

            });
        });
    </script>

    <script>
        var coll = document.getElementsByClassName("collapsible");
        var i;

        for (i = 0; i < coll.length; i++) {
            coll[i].addEventListener("click", function () {
                // Close all collapsibles
                var allCollapsibleContents = document.getElementsByClassName("content");
                for (var j = 0; j < allCollapsibleContents.length; j++) {
                    allCollapsibleContents[j].style.display = "none";
                    coll[j].classList.remove("active");
                }

                // Toggle the active class for the clicked collapsible
                this.classList.toggle("active");

                // Toggle display for the content next to the clicked collapsible
                var content = this.nextElementSibling;
                if (content.style.display === "block") {
                    content.style.display = "none";
                } else {
                    content.style.display = "block";
                }
                if (content.style.maxHeight) {
                    content.style.maxHeight = null;
                } else {
                    content.style.maxHeight = (content.scrollHeight + 100) + "px";
                }
            });
        }
    </script>
</body>

</html>